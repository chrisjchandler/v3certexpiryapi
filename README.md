These are just syntax examples

to curl a url
curl "http://localhost:8080/?url=https://www.google.com"

to curl a file path
curl "http://localhost:8080/?file_path=/var/www/http/my.crt"


to run either go build blah.go or to run go run blah.go
